<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false" />

    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission
        android:name="android.permission.WRITE_SECURE_SETTINGS"
        tools:ignore="ProtectedPermissions" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.JRelay">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ConsentActivity" android:exported="false" />
        <activity android:name=".MembershipActivity" android:exported="false" />
        <activity android:name=".MemberDetailActivity" android:exported="false" />
        <activity android:name=".AddMemberActivity" android:exported="false" />
        <activity android:name=".RateLimitActivity" android:exported="false" />
        <activity android:name=".SendGroupMessageActivity" android:exported="false" />
        <activity android:name=".DeliveryStatusActivity" android:exported="false" />
        <activity android:name=".LicenseActivity" android:exported="false" />

        <receiver
            android:name=".sms.SmsReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_SMS">
            <intent-filter>
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".sms.SmsStatusReceiver"
            android:exported="false" />

        <service
            android:name=".sms.SmsSendService"
            android:exported="false" />

    </application>

</manifest>
